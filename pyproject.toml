[project]
name = "ds-quiz"
version = "1.0.0"
description = "資料結構主題測驗網站 — 期末專題"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0,<7.0",
]

[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
